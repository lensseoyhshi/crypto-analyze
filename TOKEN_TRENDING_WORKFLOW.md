# Token Trending 完整工作流程

## 📊 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Birdeye Token Trending                       │
│                    定时任务（每1小时执行）                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第一步: 获取热门代币列表（分页查询）                             │
│  ────────────────────────────────────────────────────────────── │
│  API: GET /defi/token_trending                                  │
│  参数: offset=0, limit=20, interval=24h                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第二步: 保存原始响应                                             │
│  ────────────────────────────────────────────────────────────── │
│  表: raw_api_responses                                          │
│  字段: source, endpoint, response_json, status_code              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第三步: 保存/更新结构化数据                                      │
│  ────────────────────────────────────────────────────────────── │
│  表: birdeye_token_trending                                     │
│  逻辑: 根据 address 判断，存在则更新，否则插入                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第四步: 为每个代币创建后台任务（NEW! 🆕）                         │
│  ────────────────────────────────────────────────────────────── │
│  ┌─────────────────────┐        ┌──────────────────────────┐   │
│  │ Task 1: Security    │        │ Task 2: Transactions     │   │
│  │ 获取安全信息         │        │ 获取交易记录              │   │
│  └─────────────────────┘        └──────────────────────────┘   │
│           │                              │                      │
│           ▼                              ▼                      │
│  birdeye_token_security      birdeye_token_transactions        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第五步: 判断是否继续分页                                         │
│  ────────────────────────────────────────────────────────────── │
│  如果 len(tokens) < limit 或 page >= 50                         │
│  则停止，否则 offset += 20 继续下一页                            │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 详细流程图

### 主流程
```
开始定时任务
    │
    ├─→ [每1小时触发]
    │
    ▼
初始化变量
    offset = 0
    limit = 20
    max_pages = 50
    total_saved = 0
    │
    ▼
┌───────────────────────────────────┐
│ 循环: for page in range(max_pages)│
└───────────────────────────────────┘
    │
    ├─→ 调用 API: get_token_trending(offset, limit)
    │       │
    │       ├─→ 成功? ─→ 是
    │       │            │
    │       │            ▼
    │       │      保存原始响应
    │       │            │
    │       │            ▼
    │       │      保存结构化数据
    │       │            │
    │       │            ▼
    │       │      🆕 创建后台任务
    │       │            │
    │       │            ├─→ asyncio.create_task(security)
    │       │            └─→ asyncio.create_task(transactions)
    │       │            │
    │       │            ▼
    │       │      判断是否最后一页?
    │       │            │
    │       │            ├─→ 是 ─→ break
    │       │            └─→ 否 ─→ offset += 20, 继续
    │       │
    │       └─→ 失败? ─→ 记录错误，break
    │
    ▼
完成所有分页
    │
    ▼
记录总计: "Total saved/updated X tokens"
    │
    ▼
等待1小时 (3600秒)
    │
    └─→ 返回开始
```

### 后台任务流程

#### Task 1: Token Security
```
_fetch_token_security_async(address)
    │
    ▼
创建 BirdeyeClient
    │
    ▼
调用 API: get_token_security(address)
    │
    ├─→ 成功?
    │   │
    │   ├─→ 是
    │   │   │
    │   │   ▼
    │   │ 检查 token_address 是否存在?
    │   │   │
    │   │   ├─→ 存在 ─→ 更新记录
    │   │   │            (更新所有字段)
    │   │   │
    │   │   └─→ 不存在 ─→ 插入新记录
    │   │                 (创建新记录)
    │   │   │
    │   │   ▼
    │   │ 提交事务
    │   │   │
    │   │   ▼
    │   │ 记录日志: "Token security saved/updated"
    │   │
    │   └─→ 否 ─→ 记录警告日志
    │
    ▼
关闭 Client
    │
    ▼
完成
```

#### Task 2: Token Transactions
```
_fetch_token_transactions_async(address, limit=50)
    │
    ▼
创建 BirdeyeClient
    │
    ▼
调用 API: get_token_transactions(address, limit=50)
    │
    ├─→ 成功?
    │   │
    │   ├─→ 是
    │   │   │
    │   │   ▼
    │   │ 遍历每个交易
    │   │   │
    │   │   ├─→ 检查 txHash 是否存在?
    │   │   │   │
    │   │   │   ├─→ 存在 ─→ 更新交易
    │   │   │   │
    │   │   │   └─→ 不存在 ─→ 插入新交易
    │   │   │
    │   │   ▼
    │   │ 提交所有交易
    │   │   │
    │   │   ▼
    │   │ 记录日志: "Saved/Updated X transactions"
    │   │
    │   └─→ 否 ─→ 记录警告日志
    │
    ▼
关闭 Client
    │
    ▼
完成
```

## 📈 数据流向图

```
Birdeye API
    │
    ├─→ /defi/token_trending
    │       │
    │       ▼
    │   [原始JSON响应]
    │       │
    │       ├─→ raw_api_responses 表
    │       │   (保存原始响应)
    │       │
    │       └─→ birdeye_token_trending 表
    │           (结构化数据)
    │               │
    │               └─→ 获取每个 token.address
    │                       │
    │                       ├─────────────────┬─────────────────┐
    │                       │                 │                 │
    ├─→ /defi/token_security              │                 │
    │   (后台任务)                          │                 │
    │       │                              │                 │
    │       ▼                              │                 │
    │   birdeye_token_security 表          │                 │
    │   (安全信息)                          │                 │
    │                                      │                 │
    └─→ /defi/txs/token/seek_by_time     │                 │
        (后台任务)                          │                 │
            │                              │                 │
            ▼                              ▼                 ▼
        birdeye_token_transactions 表   Token 1          Token 2...
        (交易记录)                       (并发处理)       (并发处理)
```

## 🕐 时间线示例

```
时间轴 (假设获取20个热门代币):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

00:00  │ 启动定时任务
00:01  │ ├─ 获取第1页 (20个代币)
00:02  │ ├─ 保存原始响应
00:03  │ ├─ 保存结构化数据
00:03  │ └─ 创建40个后台任务 (20×2)
       │     ├─ Security Task 1 ──────┐
       │     ├─ Security Task 2 ──────┤
       │     ├─ ...                   ├─ 并发执行
       │     ├─ Transaction Task 1 ───┤  (2-5秒完成)
       │     ├─ Transaction Task 2 ───┤
       │     └─ ...                   ┘
00:08  │ 所有后台任务完成
       │
01:00  │ 等待1小时
       │
01:00  │ 再次启动定时任务
       └─ 重复上述流程...
```

## 💾 数据库表关系

```
birdeye_token_trending (热门代币)
    │
    │ address (PK)
    │
    ├─────────────────┬─────────────────┐
    │                 │                 │
    │                 │                 │
birdeye_token_security    birdeye_token_transactions
(安全信息)                (交易记录)
    │                     │
    │ token_address (FK)  │ base.address (JSON字段)
    │                     │
    └─────────┬───────────┘
              │
              ▼
    完整的代币分析数据
        - 基本信息 (trending)
        - 安全信息 (security)
        - 交易记录 (transactions)
```

## 🎯 并发处理示例

假设获取3个热门代币:

```
主线程:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│ 获取trending数据
│ 保存数据
│ 创建后台任务
└─→ 继续下一页

后台线程1: Token A Security
━━━━━━━━━━━━━━━━━━━━━━━━━━━
    │ API调用
    │ 保存数据
    └─→ 完成

后台线程2: Token A Transactions
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    │ API调用
    │ 保存50笔交易
    └─→ 完成

后台线程3: Token B Security
━━━━━━━━━━━━━━━━━━━━━━━━━━━
    │ API调用
    │ 保存数据
    └─→ 完成

后台线程4: Token B Transactions
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    │ API调用
    │ 保存50笔交易
    └─→ 完成

... (Token C 同样并发处理)

所有任务并行执行，互不阻塞！
```

## 📊 性能对比

### 同步方式 (旧方式):
```
Token 1 Security    (1秒)
    ↓
Token 1 Transactions (1秒)
    ↓
Token 2 Security    (1秒)
    ↓
Token 2 Transactions (1秒)
    ↓
... (总计: 20个代币 × 2秒 = 40秒)
```

### 异步方式 (新方式):
```
Token 1 Security ────┐
Token 1 Transactions ┤
Token 2 Security ────┤
Token 2 Transactions ┤ 并发执行
Token 3 Security ────┤
Token 3 Transactions ┤
... ─────────────────┘
(总计: 2-3秒) ⚡
```

**性能提升: 15-20倍！**

## 🎉 总结

通过这个完整的工作流程，您现在可以：

1. ✅ **自动获取**: 每小时自动获取最新热门代币
2. ✅ **智能更新**: 自动判断并更新已存在的代币
3. ✅ **深度分析**: 自动获取每个代币的安全信息和交易记录
4. ✅ **高效并发**: 所有后台任务并发执行，节省时间
5. ✅ **完整数据**: 一站式获得代币的全方位数据

这为您的加密货币分析提供了强大的数据基础！🚀

